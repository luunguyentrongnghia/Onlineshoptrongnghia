import * as Actiontypes from './types'
export const listHome = (Number)=>({
    type:Actiontypes.newfs,
    Number:Number
})
export const listwonmenfs = (Number)=>({
    type:Actiontypes.womenfs,
    Number:Number
})